# AnalystAI Backend Environment Variables

# Core Configuration
GOOGLE_PROJECT_ID=your-project-id
GOOGLE_LOCATION=us-central1
GCP_BUCKET=analystai-raw

# Feature Flags (set to true for production)
USE_VERTEX=false
USE_MATCHING_ENGINE=false
USE_BIGQUERY=false

# Model Configuration
GEMINI_MODEL=gemini-1.5-flash  # Primary model (fast, efficient)
VERTEX_MODEL=gemini-1.5-pro  # Vertex AI for critical analysis
EMBED_MODEL=text-embedding-004

# Security
API_KEY=your-secure-api-key

# Gemini API Key (get from https://makersuite.google.com/app/apikey)
GEMINI_API_KEY=your-gemini-api-key

# Database (optional, defaults to SQLite)
DATABASE_URL=postgresql://user:pass@localhost/analystai
SQLITE_PATH=data/analystai.db

# Server Configuration
HOST=0.0.0.0
PORT=8080
LOG_LEVEL=INFO

# Google Credentials (for Docs export and Cloud services)
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json
